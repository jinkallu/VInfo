
import { writable } from 'svelte/store';

export const  parent = writable([]);
export const self = writable({});
export const sibblings = writable([]);
export const children = writable([]);

// import { writable } from 'svelte/store';

